[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "microllm-rocm"
version = "0.1.0"
description = "Optional ctypes API for the microLLM-rocm C++/HIP engine"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}

[tool.setuptools]
packages = ["microllm"]
