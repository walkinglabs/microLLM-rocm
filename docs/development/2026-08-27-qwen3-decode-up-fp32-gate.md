# Qwen3 phase-selective decode-up FP32完整门

日期：2026-08-27
状态：显式精度策略通过，不设为默认

双表示路径完成64/64 worker、32行shape和24/24精确KV。23行直接与Transformers BF16一致，
9行仍保留`precision_mismatch`；候选在八个固定首分叉状态全部匹配PyTorch FP32 argmax。

完整向量common-oracle严格门为7/8。T128/B1中micro/PyTorch FP32 Max/RMS为
`2.346e-4/4.628e-5`，略超`2e-4/4e-5`，但两者argmax和候选argmax都是320。这是原有FP32
实现边界，必须保留，不能把8/8 argmax写成8/8完整向量精度。

五场景性能全部通过：比值为0.9712、0.9707、0.9790、1.0011、0.9774，几何均值0.97984。
T512/B2 prefill从全局候选0.8875恢复到1.0011。所有增量峰值相同；固定常驻增加336MiB。
因为首轮T128有一次慢样本，又在另一张VF完整重复30进程；复测5/5、几何均值0.98178，
T128候选三次稳定。两套共60进程与10/10 case-gates都保留。

因此keep显式CLI/API，用于需要这八个敏感状态答案的实验；默认all-BF16不变。下一步不是继续
调整简单投影，而是扩大prompt/上下文验证或进入训练/其他硬件尺度。
