# Qwen3 三步完整状态轨迹

日期：2026-08-27

单步对齐像确认两个人从同一起点迈出第一步；三步审计开始检查方向是否持续一致。每一步比较loss，第3步后比较310个参数、620个AdamW moment和step=3。

FP32六道门全过：loss最大差`4.470e-6`，参数Max/RMS `3.227e-5/5.161e-8`，moment `7.258e-5/4.584e-8`。两边loss都从约2.3768降到0.3520。

BF16的loss最大差`0.01167`，参数RMS `3.995e-6`，moment Max `0.1407`，三道门失败。最坏moment从一步的embedding传播到block2 FFN down。全部值有限且step=3，属于数值轨迹分叉而非执行失败。

每种精度14.31GB临时状态已删除；930条raw记录保留。更长轨迹、checkpoint中断恢复和BF16 moment存储仍分开。
