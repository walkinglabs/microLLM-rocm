# 两种Grouped都做完以后，时间还在哪里

日期：2026-08-24

QKV从三次提交变一次，gate/up从两次变一次。Qwen每层少三次，共少72次；DeepSeek共少84次。

结果：

| 模型 | GEMM次数 | GEMM时间 | 全部Kernel时间 |
|---|---:|---:|---:|
| Qwen | 217→145 | 快1.182倍 | 快1.009倍 |
| DeepSeek | 253→169 | 快1.099倍 | 快1.034倍 |

GEMM仍占约47%和59%，但能“读取同一输入、彼此独立”的projection已经组合完了。下一步不能
随便把有依赖关系的算子放在一起。

cast和strided copy合计还占约19%和15%，softmax也每层运行。新的设计需要跨过更大的Attention
边界，同时继续检查完整logits、显存和端到端速度。
