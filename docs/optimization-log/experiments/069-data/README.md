# Experiment 069 evidence

- `raw.jsonl`: 72 microLLM-only records; uniform and strict policy order alternates per run.
- `summary.json`: 12 complete shape rows with same-binary median ratios.
- `build-contract.json`: frozen Release protocol.

This experiment replaces cross-window performance attribution in Experiment 067. It does not replace
Experiment 067's complete-logit evidence or Cache byte accounting.
