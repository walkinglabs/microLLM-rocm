# Experiment 045 evidence

- `pilot-before-load.jsonl`: first one-process matrix before the loading redesign.
- `candidate/raw.jsonl`: four shapes × two frameworks × three fresh processes after it.
- `candidate/summary.json`: formal medians.
- `load-summary.json`: per-shape load medians and setup interpretation.

The before pilot did not yet expose `load_ms`; its roughly 6–7 minute process setup was
observed from process elapsed time during the run and is reported as a range, not as an
invented exact duration.
