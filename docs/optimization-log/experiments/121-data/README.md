# Experiment 121 data

raw hipBLASLt `INT8×INT8→INT32` square GEMM executed probe。

```text
sizes        128,256,512,1024,2048,4096
warmup       5
repetitions  20
accuracy     5 CPU integer samples per shape, exact
timing       HIP Event
```

该目录不证明公共 Tensor 或 Transformer INT8 支持。
